# FitX Web — UI Kit

Marketing website. Hero → programs → conditions → transformations → enrolment.

- `index.html` — interactive landing page
- `Web.jsx` — sections (Nav, Hero, Programs, Conditions, Stories, HowItWorks, CTA, Footer)
- Uses tokens from `../../colors_and_type.css`.
